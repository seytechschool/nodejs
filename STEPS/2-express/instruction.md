this folder requires restarting server every time we change the code.

show it by changing hello world to a message: 'please use /api for api requests'

explain that we usually use /api for api requests.
